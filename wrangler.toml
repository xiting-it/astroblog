# Cloudflare Pages Configuration
name = "astroblog"
compatibility_date = "2024-01-01"

[env.production]
routes = [
  { pattern = "/*", zone_name = "xitingit.top" }
]

[build]
command = "npm run build"
cwd = "/"
watch_paths = []

[build.environment]
NODE_VERSION = "18"
